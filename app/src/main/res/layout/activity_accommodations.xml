<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="16dp"
    android:paddingRight="16dp"
    android:id = "@+id/accommodationsActivity">

    <Spinner android:id="@+id/spAccommodationType"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_marginTop="5dp"
        />
    <ListView
        android:id="@+id/accommodationsListView"
        android:layout_width="match_parent"
        android:layout_height="500dp"
        android:layout_alignParentStart="true"
        android:layout_below="@id/spAccommodationType" />

    <Button
        android:id="@+id/bAccommodationAdd"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/accommodationsListView"
        android:text="Add new accommodation"
        android:layout_centerHorizontal="true"/>

</RelativeLayout>